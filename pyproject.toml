[project]
name = "winclip"
version = "0.1.0"
description = "A GTK4 clipboard manager daemon for Linux"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
winclip = "main:main"

[tool.ruff.lint.per-file-ignores]
"gui/**" = ["E402"]
"app/**" = ["E402"]
